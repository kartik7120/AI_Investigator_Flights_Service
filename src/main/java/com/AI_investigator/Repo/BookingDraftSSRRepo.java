package com.AI_investigator.Repo;

import com.AI_investigator.model.BookingDraftSSR;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BookingDraftSSRRepo extends JpaRepository<BookingDraftSSR, Long> {
}
